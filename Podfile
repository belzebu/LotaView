# MobileVLCKit removed — using native RTSP client + VideoToolbox
# Podfile kept for reference in case fallback is needed.

platform :ios, '17.0'

target 'RTSPViewer' do
  # No pods — all native frameworks
end
